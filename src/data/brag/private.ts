import type { BragEntry, BragSummary } from '~/types/brag'

// Production-safe defaults. Keep empty in git-tracked code.
export const privateBragEntries: BragEntry[] = []

export const privateSummaries: BragSummary[] = []
